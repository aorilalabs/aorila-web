# Local / deploy env (no secrets committed)

# Optional paths (defaults under ./data)
# LEADS_PATH=
# VENDORS_PATH=

# Required to call /api/ops/vendors* (Bearer token)
# VENDOR_OPS_TOKEN=

# PORT=3000
